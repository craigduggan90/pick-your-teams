# Pick Your Teams — UI

React + Vite + TypeScript frontend. See [`docs/claude/stage-1.md`](../../docs/claude/stage-1.md)
and the project root's `claude.md` for build context and conventions.

## Commands

```sh
npm run dev    # start the dev server
npm run build  # typecheck and build
npm run test   # run the Vitest suite
npm run lint   # oxlint
```

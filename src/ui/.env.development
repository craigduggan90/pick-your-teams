# Local dev proxy target — see docs/local-dev-topology.md. Points at Teams.DevGateway, which
# calls Teams.Authoriser and injects the Teams-User-* headers Teams.Api requires. Not a secret,
# committed for every developer.
VITE_PROXY_TARGET=http://localhost:5200

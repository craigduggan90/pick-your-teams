# Stand-in until the real AWS API Gateway is deployed — see docs/local-dev-topology.md. Update
# this to the real API Gateway URL once one exists. Until then it points straight at Teams.Api:
# protected calls will 401 (no Teams-User-* headers get set on this path), but it proves the
# dev/prod target switch itself works, and unauthenticated behaviour can be sanity-checked.
VITE_PROXY_TARGET=http://localhost:5199
